name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Run AI review
        # Pinned to a commit SHA (not @master) so a future push to that
        # branch can't silently change what runs in this CI job with
        # access to ANTHROPIC_API_KEY / GITHUB_TOKEN. Bump deliberately:
        # https://github.com/NivL1/ai-pr-reviewer/commit/565feb257f8c481c568e616f2b8b17b130ee795c
        uses: NivL1/ai-pr-reviewer@565feb257f8c481c568e616f2b8b17b130ee795c
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GITHUB_SHA: ${{ github.event.pull_request.head.sha }}
